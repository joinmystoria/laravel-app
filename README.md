This is a simple CRUD using Php Laravel & postgreSQL.

Note:
When cloning on local run the ff cmd: 
* composer install
* php artisan key:generate
* php artisan migrate

Local DB Configuration is under .env